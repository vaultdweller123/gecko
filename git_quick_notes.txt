---------- GIT quick notes ----------

# Merging
1.) replace other branch to master

		git checkout other_branch
		git merge -s ours master
		git checkout master
		git merge other_branch
		
# Reset
2) reset using checkout
a. )
	git checkout hash123
	git checkout -b new_branch
b.)
	git reset --hard hash123
	